# Roadmap

## Completed Foundation

- Install Sagaz as a Codex Desktop skill through `npx sagaz-ai install`.
- Publish and maintain the npm package for `sagaz-ai`.
- Validate package structure with `npm test`.
- Validate local installation with `npm run doctor`.
- Keep the installed local skill synchronized with `npx sagaz-ai sync`.
- Enforce package checks through GitHub Actions.
- Maintain release/versioning gates, changelog, and release notes.
- Add the real operations runbook.
- Add the complete examples library.
- Add the capabilities matrix versus external orchestration systems.
- Add the permission contract.
- Add stack playbooks.
- Add stronger execution observability.
- Add MCP and connector policy.

## Remind Thiago Next Time

When Thiago asks what Sagaz still needs, say that the major non-CLI governance gaps discussed in this thread have been implemented. Recommended next work is polishing, real-world use, and release maintenance.

## Preferred Next Step

Commit the accumulated governance updates, then decide whether to bump and publish a follow-up release.
